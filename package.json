{
  "name": "hb-online-p2p-v16",
  "version": "16.0.0",
  "private": true,
  "description": "Juego web multijugador con física HB Local V14, host de sala autoritativo y gameplay P2P por WebRTC.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/engine.js && node --check public/stadiums.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  }
}
